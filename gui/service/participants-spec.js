describe('participantService', function() {

  beforeEach(module('toolkit-gui'));

  it('should ...', inject(function(participantService) {

	//expect(participants.doSomething()).toEqual('something');

  }));

});